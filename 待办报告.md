# MOSAIC 拼图工具 · 待办报告（TODO Report）

> 生成日期：2026-07-10 ｜ 依据：2026-07-09 ~ 07-10 的技术栈体检（技术总监视角）与功能讨论
> 使用说明：本报告汇总「当前检验出的问题点」与「规划新增的功能」，按 **P0 / P1 / P2** 排序。
> - **P0** = 阻塞级，不处理会拖垮长期维护/迭代，需最先决断
> - **P1** = 开工即做的质量与体验基线
> - **P2** = 择机推进的增强项

---

## 一、当前状态摘要

- **核心栈**：React 18 + TypeScript + Vite 6 + zustand + Tailwind CSS 3 + @dnd-kit + exifr + react-router-dom v7 + lucide-react
- **已完成功能**：上传（点击/双击/拖拽/粘贴）、EXIF 方向修正、拖拽排序、拼接配置（方向/间距/背景/透明/格式/质量）、Canvas 拼接导出、预览（缩放/平移）、下载、主题切换
- **额外交付**：单文件 HTML（`拼图工具.html`，可双击离线打开；入口已与 dev 统一，`App.tsx` 运行时按协议自动选 HashRouter/BrowserRouter）
- **类型检查**：`tsc -b --noEmit` 通过
- **关键缺口**：零测试、无 CI（P0 #1 git、#2 厂牌、#3 双入口统一已于 2026-08-07 处理并推送 GitHub）

---

## 二、问题点（技术债 / 工程化缺口）

### P0 — 阻塞级（必须最先处理）

| # | 问题 | 影响 | 建议处理 |
|---|---|---|---|
| 1 | **无 git 仓库** | 无法版本管理、协作、回滚 | `git init` + 推远端 | ✅ **已做**：`git init -b main` + 首次提交 `d704134`；✅ 已推送 GitHub `waift/mosaic-pintu-tool`（public） |
| 2 | **绑定厂牌私有插件** `vite-plugin-trae-solo-badge` | 注入 "Made with Trae" 角标；长期项目不应依赖单一 IDE 私货，存在锁定风险 | 移除插件 + 移除 `babel-plugin-react-dev-locator`（注入 `data-trae-inspector-*` 属性，同为厂牌残留） | ✅ **已做**：两插件已从 `vite.config.ts` / `package.json` 移除，重建产物零 trae 残留，bundle 400KB→361KB |
| 3 | **双入口分叉**：`src/main.tsx`(BrowserRouter) + `src/single.tsx`(HashRouter) | 为单文件 HTML 平行一套入口，长期必裂、逻辑重复 | 统一路由策略 + 构建开关（`define`）区分部署/单文件 | ✅ **已做**：删除 `src/single.tsx`，`App.tsx` 按 `window.location.protocol` 运行时选择路由（file:→HashRouter，其余→BrowserRouter），dev 与单文件共用 `main.tsx`+`App.tsx`；`single.html` 改为引用 `/src/main.tsx`；包名 `trae-project`→`mosaic-tool`，新增 `build:single` 脚本 |
| 4 | **产品形态未定**（A 离线单文件 / B 真网站 / C 两者） | 架构分叉根源，决定路由/交付/功能边界 | 先拍板再动代码（见第四节决策清单） |

### P1 — 开工即做（质量与体验基线）

| # | 问题 | 影响 | 建议处理 |
|---|---|---|---|
| 5 | **无顶层 ErrorBoundary** | 运行时异常直接白屏 | 用 React ErrorBoundary 包裹 `<App/>` |
| 6 | **零自动化测试** | 回归无保障，迭代易碎 | 加 Vitest + @testing-library/react；优先补 `utils/image.ts` 纯函数（方向计算 / 布局 / 尺寸校验） |
| 7 | **无代码规范约束** | 格式/提交无统一标准 | 加 Prettier + lint-staged + 基础 CI（如 GitHub Actions） |
| 8 | **zustand 单体 store 将随功能膨胀** | 状态散、难维护、撤销重做难接 | 按 slice 拆分；撤销重做接入 `zundo` 中间件 |

### P2 — 择机推进（增强项）

| # | 问题 | 建议处理 |
|---|---|---|
| 9 | react-router v7 对单页过重 | 保持单页可移除；若规划多页则保留并完善 |
| 10 | 图片处理全手写 Canvas（`utils/image.ts`） | 复杂编辑前抽象「画布层」；评估引入 Konva / Fabric |
| 11 | 性能：主线程重复解码大图、无 Web Worker / OffscreenCanvas | 大图场景卡顿；后续用 Worker 解码、离屏渲染 |
| 12 | 无 i18n | 若出海再加；当前中文可暂缓 |
| 13 | 依赖可升级（React 19 / Tailwind 4 / Vite 7） | 择机，非紧急 |

---

## 三、新增功能（产品迭代）

> 来源：2026-07-09 功能讨论。下列为「功能」本身，工程化问题见第二节。

### P0 — 最先做（价值最高）

| # | 功能 | 说明 |
|---|---|---|
| 1 | **单张图片编辑** | 旋转/翻转（90/180/270/任意角）、裁剪（自由 + 比例 1:1/4:3/9:16）、亮度/对比度/饱和度、滤镜预设（复古/黑白/清新）。当前完全空白，投入产出比最高 |
| | ⚠️ 前置建议 | 先引入画布抽象层（Konva）再上功能，避免手写 Canvas 膨胀 |

### P1 — 高价值迭代

| # | 功能 | 说明 |
|---|---|---|
| 2 | **撤销 / 重做** | 基于 zustand + zundo 历史栈；删除/排序/改配置/编辑均可回退 |
| 3 | **网格 / 模板拼图** | 九宫格、品字、田字、海报、心形等预设布局 + 裁剪填充（crop to uniform），让「拼图」名副其实 |
| 4 | **下载增强** | 自定义文件名、导出 PDF、按目标体积自动调质量（如控制在 2MB 内） |
| 5 | **实时预览** | 拖动配置时右侧实时重算，免去每次点「生成」 |

### P2 — 增强 / 探索

| # | 功能 | 说明 |
|---|---|---|
| 6 | 水印 | 文字（位置/透明度/旋转）+ logo 图片 |
| 7 | 长图切割 | 超长图按高度切片（适配微博/微信） |
| 8 | 自动排序 | 按文件名 / EXIF 拍摄时间 |
| 9 | 自由画布 | 每张图拖拽定位 + 缩放 + 旋转 + 边框（接近 Canva） |
| 10 | 工程保存/读取 | 工程存为 `.mosaic` 文件，下次续编（图片本地，隐私 OK） |
| 11 | 工程化/多端 | 移动端响应式、PWA、Tauri 桌面版（脱离单文件 HTML 限制） |
| 12 | AI 辅助（抠图/智能构图） | 需 wasm 模型或联网，单文件版（`file://`）受限 |

---

## 四、决策清单（开工前需拍板）

- [x] **产品形态**：C 两者都要（dev 用 BrowserRouter，单文件用 HashRouter，由 `App.tsx` 运行时按协议自动切换）
- [ ] **是否引入画布库**（Konva / Fabric）做编辑层
- [ ] **路由策略**：保留 react-router 还是移除（取决于产品形态）
- [ ] **是否择机升** React 19 / Tailwind 4（非紧急）

---

## 五、建议执行顺序

1. **拍板产品形态**（第四节决策清单）
2. **处理 P0 问题**：git 初始化 / 去厂牌插件 / 统一双入口
3. **P1 工程化基线**：ErrorBoundary / 测试 / Prettier+CI / store 拆分
4. **P0 功能**：单张图片编辑（配合画布抽象层）
5. **P1 功能**：撤销重做 / 网格模板拼图 / 下载增强 / 实时预览
6. **滚动推进 P2**

---

---

## 六、进度记录（2026-08-07）

### P0 #1 git 初始化 — 本地部分完成
- 已执行：`git init -b main`、补 `.gitignore`（新增 `dist-single/`、`拼图工具.html`）、首次提交 `d704134`
- 提交内容：38 个源/配置文件（含 `package-lock.json`）；`node_modules` / `dist` / `dist-single` / `拼图工具.html` 均被忽略
- ✅ **GitHub 远端已推送**：`https://github.com/waift/mosaic-pintu-tool`（public 仓库，用户名 waift），`git push -u origin main` 已成功

### P0 #2 去厂牌 — 完成
- 移除 `vite-plugin-trae-solo-badge`（角标）与 `babel-plugin-react-dev-locator`（注入 `data-trae-inspector-*` 属性，同为厂牌残留）
- 涉及文件：`vite.config.ts`、`package.json`
- 验证：`npm run build` 通过，产物**零 trae 残留**，bundle 400.75KB → 361.27KB

### GitHub 远端推送步骤（待你执行）
```bash
# 方式一：网页建仓后推送（最简单，无需装软件）
# 1. 打开 https://github.com/new 新建仓库（建议名 mosaic-tool，选 Private）
# 2. 复制仓库 SSH/HTTPS 地址，然后执行：
cd "/Users/wubingyong/Desktop/bingyong 学习/workbuddy+拼图/拼图工具"
git remote add origin <粘贴仓库地址>
git push -u origin main

# 方式二：装 gh 后一条命令建仓+推送
# brew install gh && gh auth login
# gh repo create mosaic-tool --private --source . --remote origin --push
```

### 2026-08-07 晚 — P0 #3 统一入口 + 改包名（已完成）
- **统一入口**：`src/App.tsx` 改为按 `window.location.protocol` 运行时选择路由
  - `file:`（双击打开的单文件）→ `HashRouter`
  - `http(s):`（dev / 部署）→ `BrowserRouter`
  - 删除了 `src/single.tsx`；`single.html` 的脚本引用从 `/src/single.tsx` 改为 `/src/main.tsx`；dev 与单文件现在共用同一份源码，构建配置无需分叉
- **改包名**：`package.json` 的 `"name": "trae-project"` → `"mosaic-tool"`
- **新增脚本**：`npm run build:single`（等价于 `vite build --config vite.singlefile.config.ts`），构建后把 `dist-single/single.html` 拷为根目录 `拼图工具.html` 即可双击使用
- **验证**：`tsc -b --noEmit` 通过；单文件重建 385 KB，零外部引用（完全自包含）
- 涉及文件：`src/App.tsx`、`single.html`、`package.json`、`src/single.tsx`(删除)、`待办报告.md`
- 提交说明：本批改动尚未 push（需你 `git push` 或我后续推送）

*本报告为规划文档。后续按勾选进度更新此文件即可。*
