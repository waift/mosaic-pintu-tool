# 2026-07-09 工作日志

## 项目识别与本地使用
- 识别文件夹项目为「MOSAIC · 拼图工具」：React18+TS+Vite6 纯前端本地长图拼接工具，`tsc -b --noEmit` 通过。
- 启动 dev server（端口 5174，因 5173 被占用）。用户反馈 localhost 在浏览器打不开（沙箱无法访问 localhost）。

## 打包单文件 HTML（核心交付）
- 目标：双击即可打开、无需服务器、保留所有原文件。
- 新增文件（不修改原文件）：`single.html`、`src/single.tsx`（用 HashRouter 兼容 file://）、`vite.singlefile.config.ts`。
- 用 `npm install vite-plugin-singlefile --no-save` 安装内联插件（不写入 package.json）。
- 构建：`vite build --config vite.singlefile.config.ts` → 产物 `dist-single/single.html`（384KB，已内联 JS/CSS）。
- 复制到项目根：`拼图工具.html`（375K），校验无任何外部 .js/.css/http 引用，完全自包含。
- 说明：原 `App.tsx`/`main.tsx`/`index.html` 等均未改动。
