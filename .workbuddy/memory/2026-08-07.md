# 2026-08-07

## 处理 P0 #1 + #2（git 初始化 + 去厂牌）
- `git init -b main` + 首次提交 `d704134`（"init: 拼图工具初始版本（去除 Trae 厂牌角标）"）
- 补 `.gitignore`：新增 `dist-single/`、`拼图工具.html`（构建产物不入库）
- 去厂牌：移除 `vite-plugin-trae-solo-badge`（角标）与 `babel-plugin-react-dev-locator`（注入 `data-trae-inspector-*` 属性，同为厂牌残留）
  - 改 `vite.config.ts`（去 import/插件块/ babel 插件）与 `package.json`（去两个 devDep）
  - 验证：`npm run build` 通过，产物零 trae 残留，bundle 400.75KB→361.27KB
- GitHub 远端未推送：本机未装 `gh` CLI，且需用户账号授权。已在待办报告第六节给出推送命令（网页建仓 / gh 两种方式）
- 待办报告.md 已更新 P0 #1、#2 状态与进度记录、附录命令
- 用户已成功 push 到 GitHub：https://github.com/waift/mosaic-pintu-tool （public 仓库，用户名 waift）
- 注：`package.json` 的 `"name": "trae-project"` 仍是脚手架默认名，未改（非角标，可择机改名）

## GitHub 远端（22:38）
- 远端仓库：https://github.com/waift/mosaic-pintu-tool （用户建为 **Public**）
- 已执行 `git remote add origin https://github.com/waift/mosaic-pintu-tool.git`（分支 main）
- 推送待用户自己执行：`git push -u origin main`（用 fine-grained/classic token 当密码；本机无 SSH key、无凭证缓存，需用户交互输入）
- 用户不熟悉 GitHub：已逐步教 token 权限（经典 `repo` 复选框 / fine-grained 的 Contents: Read and write）、HTTPS 地址获取、private/public 区别

## 处理 P0 #3 统一入口 + 改包名（22:46 后）
- 统一入口：`src/App.tsx` 改为按 `window.location.protocol` 运行时选路由（file:→HashRouter，其余→BrowserRouter）
- 删除 `src/single.tsx`；`single.html` 脚本引用改 `/src/main.tsx`；dev 与单文件共用同一份源码（构建配置无需分叉）
- 改包名 `package.json` name: trae-project→mosaic-tool；新增 `build:single` 脚本
- 验证：`tsc -b --noEmit` 通过；单文件重建 385KB 零外部引用；已 cp 为根目录 `拼图工具.html`
- 待办报告.md 更新：P0 #1 标记已推送、#3 标记已完成、产品形态决策勾选 C、进度记录追加本批
- 提交 `bd06e50`（本地 main，领先 origin/main 一个提交，待用户 push）

## GitHub Pages 部署（22:53 后）
- 用户希望他人通过网址使用工具 → 选 GitHub Pages
- `vite.config.ts` 增 `base: './'`（已本地验证 dist 资源为相对路径 `./assets/...`）
- 新增 `.github/workflows/deploy.yml`（push/main 自动构建发布 Pages）
- 提交 `55ecc66`（本地 main 现领先 origin/main 2 个提交：bd06e50、55ecc66）
- ✅ 用户已成功 push 两次：首次 push 源码（d704134，缺 workflow 权限被拒 deploy.yml）；补 `workflow` 权限后二次 push 成功（d704134..55ecc66 main->main）
- ✅ GitHub Pages 部署成功：https://waift.github.io/mosaic-pintu-tool/
  - 首次运行失败：Pages 未启用 → 加 enablement:true 修复
  - 二次部署成功但页面空白：BrowserRouter 在子路径不匹配 → 统一改 HashRouter（提交 3a001dd）修复
  - 最终 build+deploy 全绿，网页内容验证正常
- 注：`vite-plugin-singlefile` 是 --no-save 装的，不在 package.json；Pages 部署走 vite.config.ts（不用该插件），`npm ci` 不受影响

## 更新规范（23:30）+ README（23:49）
- 写进 README：项目说明 + 开发与更新规范（main=生产/自动部署、分支策略、不动 base:'./'与 HashRouter、双产物同步、依赖/权限/自测）
- 提交 850a898 已推 main

## 后续功能规划（23:56，未实现）
- 「一键清空图片」确认：方案 A（只清 images 保留 config）、不加确认弹窗、按钮放图片列表标题栏
- 新增「图片管理弹窗」：缩略图太多需横拉 → 弹窗网格平铺全部图 + 拖拽重排（复用 @dnd-kit grid 策略）；入口按钮与清空并列放标题栏
- 弹窗追加「批量」能力：多选 + 批量删除 + 批量拖拽（选中整组后一起移动位置）；实现细节待与用户确认
- 计划：开 feature 分支；store 新增 clearImages + managerOpen 状态；新建 ImageManagerModal.tsx；网站与单文件同步
